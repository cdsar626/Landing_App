---
---

<section id="features" class="features-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title" data-i18n="features.title">Rediseñando las <span class="gradient-text">Citas</span></h2>
      <p class="section-desc" data-i18n="features.desc">Olvídate del swipe infinito. Solo lo que importa.</p>
    </div>

    <div class="features-grid">
      <!-- Feature 1 -->
      <div class="feature-card">
        <div class="icon-box">🎭</div>
        <h3 data-i18n="feat1.title">Identidad Visual</h3>
        <p data-i18n="feat1.desc">Cero prejuicios. Tu primera impresión es un Avatar o Mascota personalizable que evoluciona contigo.</p>
      </div>

      <!-- Feature 2: CORE -->
      <div class="feature-card brand-highlight">
        <div class="icon-box">🎮</div>
        <h3 data-i18n="feat2.title">Minijuego por Turnos</h3>
        <p data-i18n="feat2.desc">El corazón de la app. Rompe el hielo con <i>"¿Qué preferirías?"</i> de forma lúdica y sin presiones.</p>
      </div>

      <!-- Feature 3 -->
      <div class="feature-card">
        <div class="icon-box">🤖</div>
        <h3 data-i18n="feat3.title">Matchmaking IA</h3>
        <p data-i18n="feat3.desc">Sin búsquedas manuales. Nuestro algoritmo híbrido (Tags + IA) encuentra a tu persona ideal automáticamente.</p>
      </div>

      <!-- Feature 4 -->
      <div class="feature-card">
        <div class="icon-box">🚀</div>
        <h3 data-i18n="feat4.title">Progresión por Afinidad</h3>
        <p data-i18n="feat4.desc">La confianza se gana. Sube de nivel para desbloquear el <b>Chat</b>, enviar <b>Fotos</b> y acceder a <b>La Bóveda</b>.</p>
      </div>

      <!-- Feature 5: EXTRA -->
      <div class="feature-card">
        <div class="icon-box">🎡</div>
        <h3 data-i18n="feat5.title">Actividades Extras</h3>
        <p data-i18n="feat5.desc">
          <span class="future-games">Próximamente:</span> Desbloquea juegos clásicos para <b>hacer amigos jugando como cuando niños</b>. 
          Tic-Tac-Toe, Ahorcado, Memoria y más para afianzar el vínculo.
        </p>
      </div>

      <!-- Feature 6: LAST -->
      <div class="feature-card">
        <div class="icon-box">⏳</div>
        <h3 data-i18n="feat6.title">Daily Drops Limitados</h3>
        <p data-i18n="feat6.desc">Calidad > Cantidad. Solo recibirás 1 o 2 matches al día. Cada conexión es una oportunidad escasa que debes valorar.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .features-section {
    padding: var(--section-spacing) 0;
    position: relative;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .section-desc {
    color: var(--color-text-muted);
    font-size: 1.125rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    padding: 2rem;
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-5px);
    border-color: var(--color-primary);
    background: rgba(255, 255, 255, 0.05);
  }

  /* Highlight for the Minigame card */
  .feature-card.brand-highlight {
    border-color: rgba(139, 92, 246, 0.4);
    box-shadow: 0 0 30px rgba(139, 92, 246, 0.05);
  }

  .icon-box {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    display: inline-block;
    padding: 1rem;
    background: rgba(139, 92, 246, 0.1);
    border-radius: 50%;
  }

  h3 {
    margin-bottom: 0.5rem;
    font-size: 1.25rem;
  }

  p {
    color: var(--color-text-muted);
    font-size: 0.95rem;
  }

  .future-games {
    display: block;
    margin-top: 0.75rem;
    font-size: 0.85rem;
    color: var(--color-accent);
    font-style: italic;
  }
</style>
